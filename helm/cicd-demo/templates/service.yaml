apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-cicd-demo

spec:
  type: {{ .Values.service.type }}

  selector:
    app: cicd-demo

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
